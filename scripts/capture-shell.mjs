import {chromium} from 'playwright';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const dir=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const browser=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});const page=await browser.newPage({viewport:{width:1600,height:1080}});await page.goto('http://127.0.0.1:4173');await page.waitForFunction(()=>window.apartment?.ready);await page.evaluate(()=>{const a=window.apartment;Object.assign(a.layout,{id:'BASE',name:'Обмерная основа',partitions:[],doors:[],furniture:[],rooms:[],clearances:[]});a.state.furniture=false;a.state.labels=false;a.state.dimensions=true;a.state.cut=false;a.rebuild();});await page.waitForTimeout(250);await page.locator('#viewport').screenshot({path:path.join(dir,'views/00-base-shell.png')});const data=await page.evaluate(async()=>Array.from(new Uint8Array(await window.apartment.exportModel())));fs.writeFileSync(path.join(dir,'BASE-SHELL.glb'),Buffer.from(data));await browser.close();console.log('Base shell saved');
