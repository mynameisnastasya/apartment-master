import {chromium} from 'playwright';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const dir=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const browser=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});const page=await browser.newPage({viewport:{width:1600,height:1080}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:4173');await page.waitForFunction(()=>window.apartment?.ready);for(const id of['A','B','C']){await page.locator('[data-variant="'+id+'"]').click();await page.locator('[data-mode="plan"]').click();await page.waitForTimeout(250);await page.locator('#viewport').screenshot({path:path.join(dir,'views',id+'-orthographic.png')});}await page.goto('http://127.0.0.1:4173/REPORT.html');await page.waitForLoadState('networkidle');const missing=await page.locator('img').evaluateAll(imgs=>imgs.filter(i=>i.complete&&!i.naturalWidth).map(i=>i.src));if(missing.length||errors.length)throw new Error(JSON.stringify({missing,errors}));await page.screenshot({path:path.join(dir,'views/report-preview.png')});await browser.close();console.log('Plan labels and report images verified');
